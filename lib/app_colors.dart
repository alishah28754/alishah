import 'package:flutter/material.dart';

/// K-TEX brand tokens.
/// Gold value sampled directly from the approved logo artwork.
class AppColors {
  static const primary = Color(0xFF000000); // Black — branding, buttons
  static const onPrimary = Color(0xFFFFFFFF);
  static const gold = Color(0xFFD3AF64); // Sampled from the logo artwork
  static const goldDark = Color(0xFF8A6B1F); // Text-on-gold-container tone
  static const goldContainer = Color(0xFFF4E4BC); // Soft gold background
  static const saleRed = Color(0xFFF1414D); // Sale / urgency
  static const onSaleRed = Color(0xFFFFFFFF);
  static const background = Color(0xFFFCF9F8);
  static const surfaceContainerHigh = Color(0xFFEAE7E7); // search bar bg
  static const outline = Color(0xFF7E7576);
  static const outlineVariant = Color(0xFFCFC4C5);
}

const kFont = 'Inter';

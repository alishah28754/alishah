name: ktex_home
description: "K-TEX shopping app - home page prototype"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # ============================================================
  # ASSETS SECTION - Add this to load images
  # ============================================================
  assets:
    - assets/images/
    # - assets/icons/
    # - assets/fonts/
  
  # ============================================================
  # FONTS SECTION (Optional - for Inter font)
  # ============================================================
  # fonts:
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700